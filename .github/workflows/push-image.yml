---
name: push

on:
  workflow_run:
    workflows: ["test"]
    branches: [main]
    types: 
      - completed

env:
  DOCKER_REPO: docker.pkg.github.com
  CONTAINER_VERSION: $(cat package.json | jq ".version" | tr -d "\"")
  CONTAINER_NAME: ${DOCKER_REPO?}/${{ github.actor }}/${{ github.event.repository.name }}:${CONTAINER_VERSION?}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Log in to the container registry
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          registry: ghcr.io/gelugu
          username: ${{ github.actor }}
          password: ${{ secrets.DOCKER_CONNET }}

      - name: Build container
        run: docker build . -t ${CONTAINER_NAME?}

      - name: Push container
        run: docker push ${CONTAINER_NAME?}
...
